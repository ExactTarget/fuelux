
var connect = require('./');
var app = connect();

app.use(connect.cookieParser());
app.use(connect.cookieSession({ secret: 'something' }));
app.use(function(req, res){
  req.session.count = req.session.count || 0;
  req.session.count++;
  res.end('' + req.session.count);
});

app.listen(3000)